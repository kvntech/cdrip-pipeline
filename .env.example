# Copy this file to .env (untracked, see .gitignore) and fill in real values.
# ripper_orchestrator.py reads these for the Navidrome rescan step.
# Load into your shell before running the orchestrator, e.g.:
#   set -a; source .env; set +a; python3 ripper_orchestrator.py

NAVIDROME_USER=kevin
NAVIDROME_PASS=changeme
